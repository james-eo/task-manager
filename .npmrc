localhost# Mastra Cloud npm compatibility settings
legacy-peer-deps=true
auto-install-peers=true
fund=false
audit=false

# Force npm to resolve dependencies like pnpm
strict-peer-deps=false
prefer-dedupe=true